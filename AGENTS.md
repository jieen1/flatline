# AGENTS.md · Flatline 执行规范

本文件是主 agent 与所有子 agent 在 Flatline 仓库中工作的**强制执行规范**。
任何与本文件冲突的默认行为，以本文件为准。

## 0. 产品一句话（防止跑偏）

Flatline 是一台**本地优先**的 Agent 工作历史监护仪：它把本机五个 harness（Claude Code、Codex、opencode、dsh、Hermes）写下的会话转写读成一份可下钻的事实，
主线回答两个问题——**这些会话里发生了什么**（会话管理：度量、层级、项目、时间）与**哪里反复卡住**（摩擦发现：签名、生命周期、规则覆盖缺口）；
资产生命体征（Skill / AGENTS.md / Rule / Hook 的静默、损坏、绕行、休眠）是从摩擦与会话往下钻的一层，不是入口（ADR-18）。

目标架构：**Go + 纯 Go SQLite 驱动 + 单 daemon + loopback + 内嵌 SPA**。daemon 是唯一数据属主。

## 1. 动手之前：先读文档

开始任何任务前，按任务类型读取对应设计文档（至少读一遍，不要凭记忆或猜测）：

| 任务类型 | 必读文档 |
| --- | --- |
| 架构、数据模型、detector、状态机 | `docs/flatline-system-design-v0_4.md` |
| 会话/总览/摩擦的数据层与 **API 契约** | `docs/flatline-session-first-redesign-v1.md`（当前契约见 §27 总表） |
| 摩擦页信息架构 | `docs/flatline-friction-page-design-v1.md` |
| UI、信息架构、组件、文案、颜色语义 | `docs/flatline-ui-design-guidelines-v2_0.md` |
| 阶段目标、验收标准、依赖关系 | `docs/roadmap.md` |
| 架构级决策（新增/变更/推翻） | `docs/adr/` 下相关 ADR |
| 数据源支持哪些字段 | `docs/field-matrix-*.md`（claudecode / codex / opencode / dsh） |
| 构建、测试、提交规范、每次构建后的核对流程 | `DEVELOPMENT.md`、`CONTRIBUTING.md` |

原型参考：`docs/flatline-prototype.zip` 是设计输入，**只读、不解压到生产目录**（`web/` 等）。

## 2. 证据纪律（最高优先级，不可妥协）

1. **禁止伪造数据。** 测试 fixture 必须明确标注为 fixture；任何演示数据不得混入真实数据路径。
2. **禁止因果结论。** 系统只陈述对齐（"沉默始于 CC 2.14 升级后 2 天内"），永不写"导致"。代码、文案、注释、提交信息均适用。
3. **禁止无证据健康分。** 不输出不透明质量分/总分/排行榜。每个比例必须可下钻到分子/分母/基线/样本量。
4. **缺失 ≠ 零。** 数据源未记录的环节显示"未记录"，不显示 0、空白或绿色。
5. **观测等级如实标注。** `observation_level` 使用系统设计 §3.1 定义的封闭枚举（invoked / observed-use / loaded / offered / inferred / unknown），必须随数据携带并展示；unknown 与 inferred 永不伪装成事实。参与形式（如 followed）不得冒充观测等级。
6. **判定规则必须一句话可解释**（ADR-8）：任何进入告警的阈值/算法，若无法在告警文案中用一行分子分母说清，不得实现/上线。

## 3. 写入与删除纪律

- **所有写入或删除资产的操作必须用户显式确认。** 包括"清理"——系统绝不静默修改或删除任何资产文件。
- 清理/归档必须生成**可回滚记录**（不物理删除文件，提供移除建议或 .disabled 方案）。
- 代码层面：任何对资产源文件的写操作路径，必须有显式确认参数/接口，且默认拒绝。
- 对仓库本身：不擅自 `git commit`、不擅自 `git push`——除非用户明确指示。

## 4. 工作方式

- **小任务先测试**：先写/更新测试（或最小可验证断言），再实现；测试失败时先怀疑自己，不伪造通过。
- 修改前先定位现有代码与相关 ADR；架构级变更必须先写 ADR（见 `docs/adr/README.md` 流程）。
- 派生子 agent 时：
  - **子 agent 默认只用 Haiku 或 Sonnet 模型**；只有用户就当次任务明确许可时才可用 Opus（用户 2026-08-22 已就重构线明确许可）。默认之外的每一次都要用户点头，不得由主 agent 自行放宽；
  - 任务描述必须自包含（目标、边界、必读文档、验收标准），并重申本文件第 2、3 节纪律；
  - 子 agent 的产出由主 agent 复核后才算完成。
- 不引入设计文档未承诺的依赖、模块或功能；MVP 范围以 `docs/roadmap.md` 与系统设计 §8 为准。

## 5. 交付规范

每次任务交付必须包含以下四部分，缺一不可：

1. **变更摘要**：改了什么、为什么、涉及哪些文件（绝对路径）；
2. **测试**：运行了哪些测试、结果如何（附命令与关键输出）；
3. **风险**：已知风险、未验证的假设、可能的回归面；
4. **未完成项**：明确列出没做完的部分与原因，不粉饰。

## 6. MVP 边界（当前不实现）

以下能力**不进入 MVP**，任何 agent 不得提前实现：

- 账户 / 云同步 / 团队协作；
- AI 分析层；
- 桌面壳（Electron 等）；
- 插件运行时；
- 群体参考（匿名群体参考区间）；
- 正式统计改善轮次（ImprovementCycle，已整体移入 Backlog，见系统设计附录 B）。

## 7. 隐私边界

- 本地优先：不默认上传任何本地数据；全流程不依赖登录。
- 只监听 loopback 地址；不引入遥测、不引入第三方网络请求。
- 测试与 fixture 中不得包含真实用户 Session 数据。